datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Song {
  id          Int      @id @default(autoincrement())
  title       String
  genre       String?
  collection  String?
  coverURL    String?
  artists     Artist[]
  ratings     Rating[]
}

model Artist {
  id    Int    @id @default(autoincrement())
  name  String
  songs Song[]
}

model Rating {
  id     Int  @id @default(autoincrement())
  value  Int  @db.Integer
  songId Int
  song   Song @relation(fields: [songId], references: [id])
  userId Int
  user   User @relation(fields: [userId], references: [id])

  @@unique([songId, userId])
}

model User {
  id       Int      @id @default(autoincrement())
  name     String   @unique @db.VarChar(255)
  password String   @db.VarChar(255)
  rating   Rating[]
}

// TODO: migrate to postgres
// TODO: main workflow: get songs and covers from api, once a rating for them exists a song entry is created, if no entry exists go to api